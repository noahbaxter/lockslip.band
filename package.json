{
  "name": "lockslip-band-dev",
  "private": true,
  "version": "0.0.0",
  "description": "Dev-only tooling for the Lockslip site. Not deployed; GitHub Pages serves the static files directly.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
